window_size=96#to capture both daily and monthly seasonality
batch_size=128# make sure the len of data is a multiply
n_horizon=1# forecast one step into the future
shuffle_buffer_size=1000
shift=1